/**
 * Default file (tests, SSR prerender, builds without replacement).
 * Keep `useMockApi` false so unit tests still exercise HttpClient.
 */
export const environment = {
  production: false,
  useMockApi: false,
};
